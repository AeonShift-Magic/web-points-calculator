{% extends 'admin/admin_layout.html.twig' %}

{% block title %}{{ 'admin.mtg.update.edit.title'|trans }}{% endblock %}

{% block main_content %}
    <div class="container mx-auto px-4 py-8">
        <div class="flex justify-between items-center mb-6">
            <h1>{{ 'admin.mtg.update.edit.heading'|trans }}</h1>
        </div>

        <div>
            {{ form_start(form) }}
                {{ form_widget(form) }}

                <div class="flex items-center justify-between mt-6">
                    <a href="{{ path('admin_mtg_update_index') }}" class="text-gray-600 hover:text-gray-800">
                        {{ 'admin.mtg.update.edit.back'|trans }}
                    </a>
                    <div>
                        <a href="{{ path('admin_mtg_update_show', {'id': update.id}) }}" class="mr-2 text-gray-600 hover:text-gray-800">
                            {{ 'admin.mtg.update.edit.show'|trans }}
                        </a>
                        <button type="submit" class="bg-blue-600 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded">
                            {{ 'admin.mtg.update.edit.save'|trans }}
                        </button>
                    </div>
                </div>
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}
