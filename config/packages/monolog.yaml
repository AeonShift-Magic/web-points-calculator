monolog:
    handlers:
        main:
            type: fingers_crossed
            action_level: error
            handler: stderr

        stderr:
            type: stream
            path: php://stderr
            level: error
            formatter: monolog.formatter.json

        console:
            type: console
            process_psr_3_messages: false
