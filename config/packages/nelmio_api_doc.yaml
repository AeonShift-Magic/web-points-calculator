nelmio_api_doc:
    documentation:
        info:
            title: AeonShift API
            description: API Endpoint for AeonShift values, updates and points.
            version: 1.0.0
    areas:
        default:
            path_patterns:
                - ^/api(?!/doc$)(?!/doc\.json$)
