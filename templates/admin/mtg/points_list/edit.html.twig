{% extends 'admin/admin_layout.html.twig' %}

{% block title %}{{ 'admin.mtg.pointslist.edit.title'|trans }}{% endblock %}

{% block main_content %}
    <div class="container mx-auto px-4 py-8">
        <div class="flex justify-between items-center mb-6">
            <h1>{{ 'admin.mtg.pointslist.edit.heading'|trans }}</h1>
        </div>

        <div class="bg-white shadow-md rounded-lg p-6">
            {{ form_start(form) }}
                {{ form_widget(form) }}

                <div class="flex items-center justify-between mt-6">
                    <a href="{{ path('admin_mtg_points_list_index') }}" class="text-gray-600 hover:text-gray-800">
                        {{ 'admin.mtg.pointslist.edit.back'|trans }}
                    </a>
                    <div>
                        <a href="{{ path('admin_mtg_points_list_show', {'id': points_list.id}) }}" class="mr-2 text-gray-600 hover:text-gray-800">
                            {{ 'admin.mtg.pointslist.edit.show'|trans }}
                        </a>
                        <button type="submit" class="bg-blue-600 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded">
                            {{ 'admin.mtg.pointslist.edit.save'|trans }}
                        </button>
                    </div>
                </div>
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}
